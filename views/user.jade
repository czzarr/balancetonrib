extends layouts/main

block content
  main
    .container
      img(src=friend.pic_big, alt=friend.name)
      p= friend.name
      each rib in ribs
        .rib.
          Code Banque #{rib.bank}
          Code Guichet #{rib.bank}
          Numéro de Compte #{rib.accountNumber}
          Clé RIB #{ribkey}
